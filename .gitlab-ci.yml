stages:
  - lint
  - tests
  - build

eslint:
  stage: lint
  image: node:24-alpine
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - node_modules/
      - .npm/
  before_script:
    - npm install --save-dev eslint @eslint/js typescript typescript-eslint eslint-plugin-vue vue-eslint-parser globals
  script:
    - npx eslint .

clippy:
  stage: lint
  image: gitlab.erdtcyber.com:5050/erdt-cyber/sonar:rust-clippy
  cache:
    key:
      files:
        - Cargo.lock
    paths:
      - .cargo/registry
      - target/
  script:
      - cd ./src-tauri
      - cargo clippy --all-targets --all-features
